/*
Test File to check Unity test framework
*/


#include <stdio.h>

unsigned char addition(unsigned char a, unsigned char b){
return (a+b);
}

unsigned char multiplication(unsigned char a, unsigned char b){
return (a*b);
}
